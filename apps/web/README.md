# Nekt Web App

Next.js 15 web app for Nektus — contact exchange via motion detection (bump-to-exchange).

## Development

```bash
bun run dev
```

For mobile/auth testing, use Tailscale for HTTPS — see `CLAUDE.md` for details.
