# Ignore other apps but keep monorepo structure
../../apps/web
../../.git
../../node_modules

# Ignore iOS native directory so EAS uses CNG/Prebuild
# (keeps Info.plist files accessible via git, but EAS regenerates ios/ from scratch)
ios

.expo
